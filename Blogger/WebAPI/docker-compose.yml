 version: '3.7'

 services:
  app:
    build: .
    image: webapi:latest
    ports:
     - 8000:80

  mongo:
    image: mongo:latest
    ports:
    - 27017:27017
    volumes:
    - ./db-data/mongo/:/data/db
